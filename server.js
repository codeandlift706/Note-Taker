//imports
const express = require('express');
const path = require('path');
const fs = require('fs');

// Helper method for generate unique ids
const uuid = require()

//boilerplate middleware
const PORT = 3001;
const app = express();

app.use

//server listener